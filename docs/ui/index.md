# UI Guide

This section documents what the web client **actually does today**.

If implementation and older docs differ, implementation wins. We call out gaps and limitations explicitly.

## UI/UX implementation docs

- [UI & UX Implementation Status](/ui/ux-implementation-status)
  - onboarding and first run
  - landing (`/`) vs app (`/app`) routing
  - join workspace flows (`/join/*`, modal, clipboard autofill)
  - workspace / channel / DM / thread UX
  - roles & permissions in UI
  - search and notifications
  - offline and reconnect behavior
  - known limitations
